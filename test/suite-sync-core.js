(function(){
'use strict';
const VERSION='7.0.0';
const SUPABASE_URL='https://swrbpdpotmirnmtqnuba.supabase.co';
const ANON_KEY='sb_publishable_G_ZFeUouDxg57Nne5pflfQ_cVGpdMbR';
const SMOOTH=SUPABASE_URL+'/functions/v1/smooth-function';
const DRIVE=SUPABASE_URL+'/functions/v1/istif-drive';
const K={session:'mesaha_supabase_v500_session',backupSession:'mesaha_supabase_v569_session_backup',access:'mesaha_google_access_v548',terminal:'mesaha_terminal_local_mode_v556',terminalOld:'mesaha_terminal_local_mode_v557',panel:'mesaha_panel_user_v316',settings:'cam_mesaha_ayarlar_v1',active:'mesaha_active_seflik_folder_v564',folders:'mesaha_suite_folder_cache_v4',divisions:'mesaha_suite_divisions_v4',ready:'mesaha_suite_division_ready_v4',pending:'mesaha_suite_pending_ops_v4',dirty:'mesaha_suite_dirty_v7',last:'mesaha_suite_last_full_sync_v7',drive:'mesaha_suite_drive_status_v7',records:'cam_mesaha_kayitlari_v1'};
const clean=v=>String(v==null?'':v).trim().replace(/\s+/g,' ');
const fold=v=>clean(v).toLocaleLowerCase('tr-TR').replace(/ç/g,'c').replace(/ğ/g,'g').replace(/ı/g,'i').replace(/ö/g,'o').replace(/ş/g,'s').replace(/ü/g,'u').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
const read=(k,f)=>{try{const x=JSON.parse(localStorage.getItem(k)||'null');return x==null?f:x}catch{return f}};
const write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v));return true}catch{return false}};
const now=()=>new Date().toISOString();
const num=v=>{const n=Number(String(v==null?'':v).replace(',','.'));return Number.isFinite(n)?n:0};
function session(){return read(K.session,null)||read(K.backupSession,null)||{}}
function terminal(){const t=read(K.terminal,null)||read(K.terminalOld,null)||{};return t&&t.active?t:{}}
function identity(){const s=session(),u=s.user||{},m=u.user_metadata||{},a=read(K.access,{}),p=read(K.panel,{}),t=terminal(),st=read(K.settings,{}),af=read(K.active,{});return{userId:clean(u.id||a.user_id||a.userId||t.pairedUserId||t.terminalCode||t.deviceId||'local'),name:clean(p.googleFullName||p.name||a.name||a.canonical_name||t.name||m.full_name||m.name||u.email||'Kullanıcı'),email:clean(a.email||p.googleEmail||t.pairedEmail||u.email),avatar:clean(p.googleAvatarUrl||p.avatarUrl||a.avatar_url||a.picture||t.avatarUrl||m.avatar_url||m.picture),seflik:clean(af.seflik||p.activeSeflik||p.seflik||t.seflik||st.seflik),seflikKey:clean(af.seflik_key||af.seflikKey)||fold(af.seflik||p.activeSeflik||p.seflik||t.seflik||st.seflik),bolme:clean(p.bolmeNo||t.bolmeNo||st.bolmeNo),google:!!s.access_token}}
function authHeaders(){const s=session();return{'Content-Type':'application/json',apikey:ANON_KEY,Authorization:'Bearer '+clean(s.access_token||ANON_KEY)}}
function terminalAuth(){const t=terminal();return t&&t.source==='pair_code'?{terminalCode:clean(t.terminalCode),terminalToken:clean(t.terminalToken),terminalPairedUserId:clean(t.pairedUserId),terminalPairedEmail:clean(t.pairedEmail)}:{}}
async function post(url,action,data){const body={action,source:'mesaha-suite-v7',...terminalAuth(),...(data||{})};const r=await fetch(url,{method:'POST',cache:'no-store',headers:authHeaders(),body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok||j.ok===false)throw new Error(clean(j.error||j.message)||('Sunucu hatası '+r.status));return j}
const edge=(action,data)=>post(SMOOTH,action,data);
const drive=(action,data)=>post(DRIVE,action,data);
function dirtyState(){const d=read(K.dirty,{});return d&&typeof d==='object'?d:{}}
function markDirty(app,meta){const d=dirtyState();d[app||'suite']={dirty:true,at:now(),meta:meta||{}};write(K.dirty,d);dispatch();return d}
function clearDirty(app){const d=dirtyState();if(app)delete d[app];else Object.keys(d).forEach(k=>delete d[k]);write(K.dirty,d);dispatch()}
function pendingOps(){const x=read(K.pending,[]);return Array.isArray(x)?x:[]}
function isDirty(){const d=dirtyState();return pendingOps().length>0||Object.values(d).some(x=>x&&x.dirty)}
function dispatch(){try{window.dispatchEvent(new CustomEvent('mesaha-suite:sync-state',{detail:{dirty:isDirty(),pending:pendingOps().length,state:dirtyState()}}))}catch{}}
function toast(msg,bad){if(window.MesahaSuiteUI&&typeof window.MesahaSuiteUI.toast==='function')return window.MesahaSuiteUI.toast(msg,bad);let el=document.getElementById('suiteSyncToastV7');if(!el){el=document.createElement('div');el.id='suiteSyncToastV7';el.style.cssText='position:fixed;left:14px;bottom:78px;z-index:2147483647;max-width:min(360px,calc(100vw - 28px));padding:11px 14px;border-radius:14px;background:#163f30;color:white;font:600 13px system-ui;box-shadow:0 12px 36px #0003;opacity:0;transform:translateY(8px);transition:.18s;pointer-events:none';document.body.appendChild(el)}el.textContent=msg;el.style.background=bad?'#8f2d2d':'#163f30';requestAnimationFrame(()=>{el.style.opacity='1';el.style.transform='translateY(0)'});clearTimeout(toast.t);toast.t=setTimeout(()=>{el.style.opacity='0';el.style.transform='translateY(8px)'},3600)}
let viewportBase=0;function keyboardOpen(){const a=document.activeElement,editable=!!(a&&(/^(INPUT|TEXTAREA)$/.test(a.tagName)||a.isContentEditable)),vv=window.visualViewport;if(vv){viewportBase=Math.max(viewportBase,vv.height||0);const shrink=viewportBase-(vv.height||0);return shrink>120}return editable&&window.matchMedia&&window.matchMedia('(pointer: coarse)').matches}
function installButton(){if(document.getElementById('suiteSyncFabV7'))return;const b=document.createElement('button');b.id='suiteSyncFabV7';b.type='button';b.innerHTML='<span aria-hidden="true">↻</span><b>Senkronize Et</b><small id="suiteSyncFabCountV7"></small>';b.style.cssText='position:fixed;left:max(12px,env(safe-area-inset-left));bottom:max(12px,env(safe-area-inset-bottom));z-index:2147483000;display:none;align-items:center;gap:8px;border:0;border-radius:16px;padding:12px 15px;background:#174a32;color:#fff;box-shadow:0 12px 32px rgba(17,65,43,.32);font:700 13px system-ui;touch-action:manipulation;-webkit-tap-highlight-color:transparent';b.querySelector('span').style.cssText='font-size:20px;line-height:1';b.querySelector('small').style.cssText='min-width:18px;height:18px;padding:0 5px;display:none;place-items:center;border-radius:99px;background:#fff;color:#174a32;font-size:10px';b.onclick=()=>syncAll({source:'floating-button'});document.body.appendChild(b);updateButton()}
function updateButton(){const b=document.getElementById('suiteSyncFabV7');if(!b)return;const count=pendingOps().length+Object.values(dirtyState()).filter(x=>x&&x.dirty).length;b.style.display=isDirty()&&!keyboardOpen()?'flex':'none';const c=document.getElementById('suiteSyncFabCountV7');if(c){c.textContent=String(count);c.style.display=count?'grid':'none'}}
function volume(r){const d=num(r.diameter),l=num(r.length),q=Math.max(1,num(r.quantity||1));return d&&l?Math.PI*Math.pow(d/100,2)/4*l*q:0}
function activeFolder(){const a=read(K.active,{}),fs=read(K.folders,[]);return (Array.isArray(fs)?fs:[]).find(f=>f&&!f.deleted&&(clean(f.seflik_key||f.seflikKey)===clean(a.seflik_key||a.seflikKey)||clean(f.seflik)===clean(a.seflik)))||null}
async function syncManagement(){let list=pendingOps();if(!list.length){clearDirty('suite');return{done:0,left:0}};const remain=[];let done=0;for(const item of list){try{const p=item.payload||{};if(item.type==='create_seflik')await edge('seflik_folder_create_seflik',{seflik:p.seflik});else if(item.type==='delete_seflik')await edge('seflik_folder_delete_seflik',{seflik:p.seflik,confirmSeflik:p.seflik});else if(item.type==='rename_seflik')await edge('seflik_folder_rename_seflik',{seflik:p.oldName,oldSeflik:p.oldName,newSeflik:p.newName,new_seflik:p.newName});else if(item.type==='add_member')await edge('seflik_folder_add_member',{seflik:p.seflik,member_user_id:p.member_user_id});else if(item.type==='remove_member')await edge('seflik_folder_remove_member',{seflik:p.seflik,member_user_id:p.member_user_id});else if(item.type==='create_division')await edge('seflik_folder_create_division',{seflik:p.seflik,bolmeNo:p.bolmeNo,location:p.location||''});else if(item.type==='delete_division')await edge('seflik_folder_delete_division',{seflik:p.seflik,bolmeNo:p.bolmeNo,confirmBolme:p.bolmeNo,permanent:true});done++}catch(e){item.error=clean(e.message||e);remain.push(item)}}write(K.pending,remain);if(!remain.length)clearDirty('suite');return{done,left:remain.length}}
async function syncMesaha(){const records=read(K.records,[]);if(!Array.isArray(records))return{done:0};const id=identity(),af=activeFolder(),seflik=clean(af&&af.seflik||id.seflik);if(!seflik||!records.length){clearDirty('mesaha');return{done:0}}const groups={};for(const r of records){const b=clean(r.bolmeNo||r.bolme_no||id.bolme);if(!b)continue;(groups[b]||(groups[b]=[])).push(r)}let done=0;for(const [bolme,rows] of Object.entries(groups)){await edge('seflik_folder_create_division',{seflik,bolmeNo:bolme,location:''});const token='suitev7_'+fold(seflik)+'_'+fold(bolme)+'_'+Date.now().toString(36)+'_'+rows.length;for(let i=0;i<rows.length;i+=150)await edge('seflik_folder_push',{seflik,bolmeNo:bolme,syncToken:token,records:rows.slice(i,i+150),appVersion:'Mesaha Suite V7'});let backup=null,driveError='';try{if(id.google){backup=await drive('backup_json',{seflik,appId:'mesaha',fileName:`Mesaha_${fold(seflik)}_${fold(bolme)}_${new Date().toISOString().slice(0,10)}.json`,recordCount:rows.length,totalVolume:rows.reduce((s,r)=>s+volume(r),0),payload:{schema:'mesaha-suite-v7',app:'mesaha',seflik,bolme,createdAt:now(),records:rows}})}}catch(e){driveError=clean(e.message||e)}await edge('seflik_folder_finish',{seflik,bolmeNo:bolme,syncToken:token,recordCount:rows.length,totalVolume:rows.reduce((s,r)=>s+volume(r),0),driveFileId:backup&&backup.fileId||'',driveFileName:backup&&backup.fileName||'',driveStatus:backup?'saved':(id.google?'error':'not_connected'),driveError,appVersion:'Mesaha Suite V7'});done+=rows.length}clearDirty('mesaha');return{done}}
function openDb(){return new Promise((resolve,reject)=>{const q=indexedDB.open('mesaha-istif-prototype',1);q.onerror=()=>reject(q.error);q.onupgradeneeded=()=>{const db=q.result;if(!db.objectStoreNames.contains('records'))db.createObjectStore('records',{keyPath:'id'});if(!db.objectStoreNames.contains('settings'))db.createObjectStore('settings',{keyPath:'key'})};q.onsuccess=()=>resolve(q.result)})}
async function idbAll(store){try{const db=await openDb();return await new Promise((res,rej)=>{if(!db.objectStoreNames.contains(store)){db.close();return res([])}const tx=db.transaction(store,'readonly'),q=tx.objectStore(store).getAll();q.onsuccess=()=>{const x=q.result||[];db.close();res(x)};q.onerror=()=>{db.close();rej(q.error)}})}catch{return[]}}
async function idbPut(store,value){const db=await openDb();return new Promise((res,rej)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).put(value);tx.oncomplete=()=>{db.close();res(true)};tx.onerror=()=>{db.close();rej(tx.error)}})}
function dataUrlFromPhoto(photo){if(!photo)return Promise.resolve('');if(typeof photo.dataUrl==='string')return Promise.resolve(photo.dataUrl);if(typeof photo==='string'&&photo.startsWith('data:'))return Promise.resolve(photo);const blob=photo.blob instanceof Blob?photo.blob:(photo instanceof Blob?photo:null);if(!blob)return Promise.resolve('');return new Promise((res,rej)=>{const fr=new FileReader();fr.onload=()=>res(String(fr.result||''));fr.onerror=()=>rej(fr.error);fr.readAsDataURL(blob)})}
async function syncIstif(){const all=(await idbAll('records')).filter(r=>r&&!r.isDemo),rows=all.filter(r=>r.syncStatus!=='synced');if(!rows.length){clearDirty('istif');return{done:0}}const id=identity(),syncedBySeflik={};let done=0;for(const r of rows){const seflik=clean(r.seflik||r.seflikName||id.seflik),bolme=clean(r.bolme||r.bolmeNo);if(!seflik||!bolme)throw new Error('İstif kaydında şeflik veya bölme eksik');await edge('seflik_folder_create_division',{seflik,bolmeNo:bolme,location:r.mevki||''});r.driveFiles=Array.isArray(r.driveFiles)?r.driveFiles:[];const photos=Array.isArray(r.photos)?r.photos:[];for(let i=r.driveFiles.length;i<photos.length;i++){if(!id.google)throw new Error('Fotoğraflı istifleri göndermek için Suite hesabından Drive bağlayın');const dataUrl=await dataUrlFromPhoto(photos[i]);if(!dataUrl)continue;const up=await drive('upload_photo',{seflik,recordDate:r.date||r.recordDate,bolmeNo:bolme,istifNo:r.istifNo,fileName:`${r.istifNo||'istif'}_${i+1}.jpg`,dataUrl,mimeType:(photos[i]&&photos[i].type)||'image/jpeg'});r.driveFiles.push(up);r.driveFolderId=up.folderId||r.driveFolderId;await idbPut('records',r)}await edge('istif_record_upsert',{seflik,record:{id:String(r.id),ormanci:r.ormanci||'',record_date:r.date||'',bolme_no:bolme,istif_no:r.istifNo||'',wood_type:r.type||'',ster:num(r.ster),coordinates:r.coordinates||null,mevki:r.mevki||null,description:r.description||null,barcode_no:r.barcode||null,photo_count:photos.length||r.photoCount||0,drive_folder_id:r.driveFolderId||null,drive_files:r.driveFiles||[],is_sent:!!(r.isSent||r.is_sent),sent_at:r.sentAt||null,created_at:r.createdAt||now(),updated_at:now()}});r.syncStatus='synced';r.updatedAt=now();await idbPut('records',r);(syncedBySeflik[seflik]||(syncedBySeflik[seflik]=[])).push(r);done++}if(id.google){for(const seflik of Object.keys(syncedBySeflik)){try{const payloadRows=all.filter(r=>clean(r.seflik||r.seflikName||id.seflik)===seflik).map(r=>({...r,photos:undefined}));await drive('backup_json',{seflik,appId:'istif',fileName:`Istif_${fold(seflik)}_${new Date().toISOString().slice(0,10)}.json`,recordCount:payloadRows.length,totalVolume:payloadRows.reduce((s,r)=>s+num(r.ster),0),payload:{schema:'mesaha-suite-v7',app:'istif',seflik,createdAt:now(),records:payloadRows}})}catch(e){console.warn('[suite-v7] İstif Drive yedeği oluşturulamadı',e)}}}clearDirty('istif');return{done}}
let syncing=false;
async function syncAll(opts){if(syncing)return{ok:false,busy:true};if(!navigator.onLine){toast('İnternet yok. Değişiklikler cihazda güvende.',true);return{ok:false,offline:true}}syncing=true;updateButton();toast('Tüm uygulamalar senkronize ediliyor…');try{const before=dirtyState(),force=opts&&['sync','server','manual','suite'].includes(opts.source);const management=await syncManagement();const mesaha=(force||(before.mesaha&&before.mesaha.dirty))?await syncMesaha():{done:0,skipped:true};const istif=(force||(before.istif&&before.istif.dirty))?await syncIstif():{done:0,skipped:true};write(K.last,{at:now(),management,mesaha,istif});dispatch();toast('Suite, Mesaha İO ve İstif İO senkronize edildi.');try{window.dispatchEvent(new CustomEvent('mesaha-suite:sync-complete',{detail:{management,mesaha,istif}}))}catch{}return{ok:true,management,mesaha,istif}}catch(e){toast('Senkronizasyon tamamlanamadı: '+clean(e.message||e),true);throw e}finally{syncing=false;updateButton()}}
async function driveStatus(){const id=identity();if(!id.google)return{ok:true,connected:false,googleRequired:true};const x=await drive('status',{seflik:id.seflik});write(K.drive,x);return x}
async function driveConnect(){const id=identity();if(!id.google)throw new Error('Drive bağlantısı için Google ile giriş yapın');const redirect=location.origin+location.pathname.replace(/[^/]*$/,'');const x=await drive('oauth_start',{seflik:id.seflik,redirectUri:redirect});if(!x.authorizationUrl)throw new Error('Google bağlantı adresi alınamadı');location.href=x.authorizationUrl}
async function driveFinish(code,state){const redirect=location.origin+location.pathname.replace(/[^/]*$/,'');const x=await drive('oauth_finish',{code,state,redirectUri:redirect,seflik:identity().seflik});write(K.drive,x);return x}
async function driveDisconnect(){const x=await drive('disconnect',{seflik:identity().seflik});write(K.drive,{connected:false});return x}
async function createSuiteBackup(){const id=identity();if(!id.google)throw new Error('Drive yedeği için Google ile giriş yapın');const mesaha=read(K.records,[]);const istif=(await idbAll('records')).map(r=>({...r,photos:undefined}));const payload={schema:'mesaha-suite-backup-v7',createdAt:now(),user:{id:id.userId,name:id.name,email:id.email},seflik:id.seflik,mesahaRecords:Array.isArray(mesaha)?mesaha:[],istifRecords:Array.isArray(istif)?istif:[],suite:{folders:read(K.folders,[]),divisions:read(K.divisions,{}),divisionReady:read(K.ready,{})}};const total=(payload.mesahaRecords||[]).reduce((s,r)=>s+volume(r),0);return drive('backup_json',{seflik:id.seflik,appId:'suite',fileName:`Mesaha_Suite_${fold(id.seflik||id.name)}_${new Date().toISOString().replace(/[:.]/g,'-')}.json`,recordCount:payload.mesahaRecords.length+payload.istifRecords.length,totalVolume:total,payload})}
const listBackups=()=>drive('backup_list',{seflik:identity().seflik});
const readBackup=id=>drive('backup_read',{id,seflik:identity().seflik});
const deleteBackup=id=>drive('backup_delete',{id,seflik:identity().seflik});
function watchStorage(){const orig=Storage.prototype.setItem;if(!orig.__suiteV7){const wrapped=function(k,v){const old=this.getItem(k);const out=orig.call(this,k,v);if(this===localStorage&&k===K.records&&old!==String(v))markDirty('mesaha',{key:k});return out};wrapped.__suiteV7=true;Storage.prototype.setItem=wrapped}window.addEventListener('storage',dispatch);window.addEventListener('mesaha:records-saved',()=>markDirty('mesaha'));window.addEventListener('mesaha:record-saved',()=>markDirty('mesaha'));window.addEventListener('mesaha:record-deleted',()=>markDirty('mesaha'));window.addEventListener('mesaha-istif:changed',()=>markDirty('istif'));window.addEventListener('focusin',updateButton,true);window.addEventListener('focusout',()=>setTimeout(updateButton,120),true);window.visualViewport&&window.visualViewport.addEventListener('resize',updateButton);window.addEventListener('orientationchange',()=>setTimeout(()=>{viewportBase=window.visualViewport?window.visualViewport.height:0;updateButton()},450));window.addEventListener('online',updateButton);window.addEventListener('offline',updateButton);setInterval(updateButton,900)}
window.MesahaSuiteSyncV7={version:VERSION,markDirty,clearDirty,isDirty,syncAll,driveStatus,driveConnect,driveFinish,driveDisconnect,createSuiteBackup,listBackups,readBackup,deleteBackup,identity,edge,drive,updateButton};
function boot(){installButton();watchStorage();dispatch();updateButton()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
